# Next.js Netlify Build Configuration

[build]
  command = "npm run build"
  functions = "/.next/server"
  publish = "/.next/static"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[functions]
  node_bundler = "esbuild"
